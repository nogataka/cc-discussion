# cc-discussion Environment Variables
# ====================================
# Copy this file to .env and fill in your values if needed

# Note: Codex agent uses codex-sdk-py which doesn't require an API key
# (uses your Codex subscription)

# Anthropic API Key (optional - Claude Agent SDK uses ~/.anthropic/api_key by default)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
